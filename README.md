# AUTIBLOOM Phase 13.3 Production Package

Deploy from this directory/ZIP root with `npm start`.

Health check: `/health`

Phase 13.3 includes therapist and parent email verification and password reset. Configure `APP_BASE_URL`, `RESEND_API_KEY`, and `EMAIL_FROM` before production use.

See `DEPLOY.md` for deployment, database migrations, email configuration, routing, and HTTP 505 proxy guidance.

export const REPORT_VERSION="AUTIBLOOM-REPORT-V1";
const band=s=>{if(s===null||s===undefined||s==="")return"Not available";const n=Number(s);if(!Number.isFinite(n))return"Not available";if(n>=80)return"High priority for review";if(n>=60)return"Moderate priority for review";if(n>=40)return"Monitor / review";return"Lower relative priority";};
export function buildClinicalDraft({child,assessment,scores=[]}){const grouped={};for(const s of scores){const t=s.score_type||"Domain";(grouped[t]??=[]).push({label:s.label,score:Number(s.score),band:band(s.score)});}const priorities=scores.map(s=>({label:s.label,score:s.score===null||s.score===undefined?null:Number(s.score),score_type:s.score_type})).filter(x=>Number.isFinite(x.score)).sort((a,b)=>b.score-a.score).slice(0,5);return{report_version:REPORT_VERSION,disclaimer:"This is a structured screening/clinical-review summary, not a diagnosis. Final interpretation remains the responsibility of the qualified clinician.",child:{id:child?.id||null,name:child?.name||null,date_of_birth:child?.date_of_birth||null},assessment:{id:assessment?.id||null,status:assessment?.status||null,completed_at:assessment?.completed_at||null},score_summary:{total_scales:scores.length,priorities,grouped},clinician_review:{strengths:[],priority_areas:priorities.map(x=>({area:x.label,score:x.score,interpretation:band(x.score),clinician_note:""})),recommendations:[],home_program:[],follow_up:""}}}